<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
  xmlns:android="http://schemas.android.com/apk/res/android"
  xmlns:app="http://schemas.android.com/apk/res-auto"
  xmlns:tools="http://schemas.android.com/tools"
  android:layout_width="match_parent"
  android:layout_height="match_parent">

  <TextView
    android:id="@+id/recipes"
    style="@style/RecipeSpinner"
    android:layout_width="match_parent"
    android:layout_height="?android:attr/listPreferredItemHeight"
    android:ellipsize="marquee"
    android:maxLines="1"
    android:singleLine="true"
    android:textAppearance="?android:attr/textAppearanceLargePopupMenu"/>

  <androidx.constraintlayout.widget.ConstraintLayout
    android:id="@+id/recipe_title_containter"
    android:layout_width="0dp"
    android:layout_height="wrap_content"
    app:layout_constraintBottom_toBottomOf="parent"
    app:layout_constraintStart_toStartOf="parent"
    app:layout_constraintEnd_toStartOf="@id/add_favorite"/>

  <com.google.android.material.floatingactionbutton.FloatingActionButton
    android:id="@+id/add_favorite"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    app:useCompatPadding="true"
    android:src="@drawable/ic_add_black_24dp"
    android:contentDescription="@string/add_favorite"
    android:tooltipText="@string/add_favorite"
    app:layout_constraintBottom_toBottomOf="@id/recipe_title_containter"
    app:layout_constraintEnd_toEndOf="parent"
    android:background="?android:selectableItemBackgroundBorderless"
    app:layout_constraintTop_toTopOf="@id/recipe_title_containter"
    app:layout_constraintDimensionRatio="2:3"/>

</androidx.constraintlayout.widget.ConstraintLayout>